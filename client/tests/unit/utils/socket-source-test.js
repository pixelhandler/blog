import socketSource from 'pixelhandler-blog/utils/socket-source';

module('socketSource');

// Replace this with your real tests.
test('it works', function() {
  var result = socketSource();
  ok(result);
});
